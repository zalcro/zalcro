# Valet Platform — Architecture Diagram

Generated by Zalcro. This diagram maps the full system architecture for a three-sided valet marketplace — customer, driver, and garage partner — built on a serverless AWS stack.

```mermaid

graph TD
    subgraph Clients["Client Applications"]
        custApp["Customer App\n(React Native)"]
        driverApp["Driver App\n(React Native)"]
        webPortal["Partner & Admin\nWeb Portal (React)"]
    end

    subgraph Edge["Edge & CDN"]
        cf["CloudFront CDN"]
        s3Static["S3 Static Assets"]
    end

    subgraph Gateway["API Gateway"]
        restGW["REST API\n(API Gateway)"]
        wsGW["WebSockets\n(API Gateway)"]
    end

    subgraph Auth["Auth"]
        authLambda["Auth Lambda\n+ Lambda Authorizer"]
    end

    subgraph CoreLambdas["Core Lambda Functions"]
        bookingL["Booking Lambda"]
        dispatchL["Dispatch Lambda\n(Surge Pricing)"]
        paymentL["Payment Lambda"]
        locationL["Location Lambda"]
    end

    subgraph SupportLambdas["Support Lambda Functions"]
        notifL["Notification Lambda"]
        partnerL["Partner Lambda"]
        adminL["Admin Lambda"]
        onboardL["Onboarding Lambda"]
    end

    subgraph Data["Data Layer"]
        rdsProxy["RDS Proxy"]
        rds["RDS PostgreSQL\n(Multi-AZ)"]
        s3Docs["S3\n(Docs & Receipts)"]
    end

    subgraph ExternalSvc["External Services"]
        stripe["Stripe"]
        twilio["Twilio SMS"]
        fcmApns["FCM / APNs"]
        maps["Google Maps\nApple Maps"]
        bgCheck["Background Check\nProvider"]
    end

    subgraph Ops["Ops & Monitoring"]
        cw["CloudWatch\n+ X-Ray"]
        secrets["Secrets Manager"]
    end

    custApp -->|REST| restGW
    custApp -->|WebSocket| wsGW
    driverApp -->|REST| restGW
    driverApp -->|WebSocket| wsGW
    webPortal --> cf
    cf --> s3Static
    webPortal -->|REST| restGW

    restGW --> authLambda
    authLambda --> bookingL
    authLambda --> dispatchL
    authLambda --> paymentL
    authLambda --> partnerL
    authLambda --> adminL
    authLambda --> onboardL

    wsGW --> locationL

    bookingL --> rdsProxy
    dispatchL --> rdsProxy
    paymentL --> rdsProxy
    locationL --> rdsProxy
    partnerL --> rdsProxy
    adminL --> rdsProxy
    onboardL --> rdsProxy
    rdsProxy --> rds

    paymentL --> stripe
    stripe -->|Webhooks| paymentL
    paymentL --> s3Docs

    onboardL --> bgCheck
    onboardL --> s3Docs

    bookingL --> notifL
    dispatchL --> notifL
    notifL --> twilio
    notifL --> fcmApns

    driverApp --> maps
    custApp --> maps

    CoreLambdas --> cw
    SupportLambdas --> cw
    CoreLambdas --> secrets
    SupportLambdas --> secrets

```